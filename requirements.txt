# Shared across recipes. Install with: pip install -r requirements.txt
speechrevolutions>=0.3.0

# Only needed for specific recipes:
# fastapi, uvicorn   — webhooks/webhook_receiver_fastapi.py, live-progress/live_progress_webapp.py
# boto3              — integrations/transcribe_from_s3.py
fastapi>=0.110.0
uvicorn>=0.27.0
boto3>=1.34.0
